# Production (Vercel / Netlify): set to your deployed FastAPI base URL (no trailing slash).
# VITE_API_URL=https://your-service.onrender.com

# Omit VITE_INFERENCE_ONLY or set to false for local full-stack (training + predict).
# Set to true for public demos: hides training UI and loads features on startup.
# VITE_INFERENCE_ONLY=true
